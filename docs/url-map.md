# Canonical URL and redirect map (F1)

`src/lib/links/url-map.ts` classifies source references without losing fragments, query strings, download identity, or protocol-specific destinations. It produces deterministic requested-to-target records and findings for malformed URLs and conflicting redirect observations.
